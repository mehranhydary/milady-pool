mutation CreateOrder($input: CreateOrderInput!) {
	createOrder(input: $input) {
		trader
		tickToSellAt
		zeroForOne
		amountSpecified
		poolKey {
			token0
			token1
			fee
			tickSpacing
			hooks
		}
		startTime
		deadline
		permit2Signature
		orderSignature
	}
}
