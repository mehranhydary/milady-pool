query GetOrders {
	orders {
		id
		trader
		tickToSellAt
		zeroForOne
		amountSpecified
		poolKey {
			token0
			token1
			fee
			tickSpacing
			hooks
		}
		permit2Signature
		startTime
		deadline
		orderSignature
	}
}
